// Created By Jared Davidson

import SwiftUI

extension BubbleBar {
    /// The position of the item bar indicator
    public enum ItemBarPosition {
        case center
        case top
        case bottom
    }
    
    /// The position of labels in the bubble bar
    public enum LabelPosition {
        case top
        case bottom
        case left
        case right
    }
} 